# Tripo API Key。在 https://developers.tripo3d.ai/en/keys 创建。
# 复制此文件为 server/.env 并填入真实 key —— .env 已在 .gitignore 中。
TRIPO_API_KEY=

# 服务端口（web/vite.config.ts 的 proxy 指向这里）
PORT=8787

# 本机走代理上网时填这里。Node 的 fetch 不读 macOS 系统代理，
# 不设会出现「DNS 能解析但 TCP 超时」。端口用你代理软件的实际端口。
# HTTPS_PROXY=http://127.0.0.1:7897
